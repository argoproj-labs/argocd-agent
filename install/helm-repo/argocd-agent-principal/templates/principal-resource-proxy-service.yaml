{{- if .Values.principal.resourceProxy.enabled }}
apiVersion: v1
kind: Service
metadata:
  namespace: {{ include "argocd-agent-principal.namespace" . }}
  name: {{ include "argocd-agent-principal.resourceProxyServiceName" . }}
  labels:
    {{- include "argocd-agent-principal.labels" . | nindent 4 }}
spec:
  ports:
  - name: resource-proxy
    protocol: TCP
    port: 9090
    targetPort: 9090
  selector:
    {{- include "argocd-agent-principal.selectorLabels" . | nindent 4 }}
{{- end }}
